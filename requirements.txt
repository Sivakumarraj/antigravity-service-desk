# ==============================================================================
# NOTE: This project uses uv for dependency management.
#
# Dependencies are declared in pyproject.toml — not here.
# To install, run:
#
#     uv sync
#
# This file is kept only for pip-based tooling compatibility.
# It intentionally mirrors the single runtime dependency in pyproject.toml.
# ==============================================================================

neuro-san-studio>=0.3.2
